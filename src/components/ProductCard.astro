---
import { Image } from "astro:assets";

interface Props {
  title: string;
  price?: string;
  images?: Record<string, ImageMetadata[]>;
  slug: string;
  initialColor?: string;
  sizes?: string[];
}

const {
  title,
  price = "25€",
  images = {},
  slug,
  initialColor,
  sizes = [],
} = Astro.props;

const sizeRange =
  sizes.length > 0 ? `${sizes[0]}-${sizes[sizes.length - 1]}` : "";
const selectedColorImages =
  (initialColor && images[initialColor]) ||
  images["Azul Turquesa"] ||
  Object.values(images)[0] ||
  [];
const firstImage =
  selectedColorImages.length > 0 ? selectedColorImages[0] : null;
const secondImage =
  selectedColorImages.length > 1 ? selectedColorImages[1] : null;

const productUrl = initialColor
  ? `/tienda/${slug}?color=${encodeURIComponent(initialColor)}`
  : `/tienda/${slug}`;
---

<a href={productUrl} class="group block">
  <div
    class="relative aspect-square w-full overflow-hidden bg-zinc-900 rounded-md border-2 border-gray-800 group-hover:border-[var(--color-orange-strong)] transition-all duration-500"
  >
    {
      firstImage ? (
        <>
          <Image
            src={firstImage}
            alt={title}
            widths={[240, 540, 720, 1024]}
            sizes="(max-width: 360px) 240px, (max-width: 720px) 540px, (max-width: 1024px) 720px, 1024px"
            format="webp"
            quality={80}
            class={`h-full w-full object-cover object-center transition-all duration-700 group-hover:scale-105 ${secondImage ? "group-hover:opacity-0" : "group-hover:opacity-80"}`}
          />
          {secondImage && (
            <Image
              src={secondImage}
              alt={`${title} alternate view`}
              widths={[240, 540, 720, 1024]}
              sizes="(max-width: 360px) 240px, (max-width: 720px) 540px, (max-width: 1024px) 720px, 1024px"
              format="webp"
              quality={80}
              class="absolute inset-0 h-full w-full object-cover object-center opacity-0 group-hover:opacity-100 transition-all duration-700 group-hover:scale-105"
            />
          )}
        </>
      ) : (
        <div class="h-full w-full flex items-center justify-center bg-zinc-900 text-white transition-colors duration-300">
          <span class="text-2xl font-bold text-[var(--color-orange-strong)] text-center px-4">
            {title}
          </span>
        </div>
      )
    }

    <div
      class="absolute bottom-4 right-4 opacity-0 group-hover:opacity-100 translate-y-2 group-hover:translate-y-0 transition-all duration-300 z-10"
    >
      <div class="bg-[var(--color-orange-strong)] rounded-full p-2 shadow-lg">
        <svg
          xmlns="http://www.w3.org/2000/svg"
          fill="none"
          viewBox="0 0 24 24"
          stroke-width="2"
          stroke="currentColor"
          class="w-5 h-5 text-white"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            d="M13.5 4.5 21 12m0 0-7.5 7.5M21 12H3"></path>
        </svg>
      </div>
    </div>
  </div>
  <div class="mt-5 text-center px-2">
    <h3
      class="text-xl font-bold text-white uppercase tracking-wider group-hover:text-[var(--color-orange-strong)] transition-colors duration-300"
    >
      {title}
    </h3>
    <div class="flex items-center justify-center gap-2 mt-2">
      <span
        class="text-xs text-gray-400 uppercase tracking-widest font-semibold"
        >Unisex {sizeRange ? `• ${sizeRange}` : ""}</span
      >
      <span class="w-1 h-1 bg-gray-600 rounded-full"></span>
      <p class="text-lg font-bold text-[var(--color-orange-strong)]">{price}</p>
    </div>
  </div>
</a>
